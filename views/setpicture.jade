html
  body
    form#uploadForm(enctype='multipart/form-data', action='/api/photo', method='post')
      if (user)
      input(type='file', name='#{user._id}')
      input(type='submit', value='Click to upload', name='submit')
